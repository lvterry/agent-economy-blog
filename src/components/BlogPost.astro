---
const { slug, titleZh, titleEn, excerptZh, excerptEn, date, tags } = Astro.props;
---

<article>
  <div class="lang-content lang-zh active">
    <h2><a href={`/blog/${slug}`}>{titleZh}</a></h2>
  </div>
  <div class="lang-content lang-en">
    <h2><a href={`/blog/${slug}`}>{titleEn}</a></h2>
  </div>
  <time>{date}</time>
  <div class="lang-content lang-zh active">
    <p>{excerptZh}</p>
  </div>
  <div class="lang-content lang-en">
    <p>{excerptEn}</p>
  </div>
  <div class="tags">
    {tags.map(tag => (
      <span>{tag}</span>
    ))}
  </div>
</article>
